{
  "name": "rooftopapartment",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku": "git init;git add .;heroku git:remote -a ;git commit -am 'make it better';git push heroku HEAD:master -f;"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.2",
    "node-schedule": "^2.1.0",
    "ping-wrapper": "0.0.3"
  }
}
